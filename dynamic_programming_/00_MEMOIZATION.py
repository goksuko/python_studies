#### 1. make it work
# - visualize the problem as a tree
# - implement the tree using recursion
# ** leaves are the base case
# - test it

#### 2. make it efficient
# - add a memo object
# ** keys: arguments, values: return values
# - add a base case to return memo values
# - store return values into the memo just before returning
